#include <stdio.h>

/**
 * main - to display a proper grammer
 *
 * return: 0 when is successful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
